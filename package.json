{
  "name": "ShareWisAssetLinkStatusCodeChecker",
  "version": "0.1.0",
  "description": "A small utility to fetch all asset links of a web page and check their HTTP Status Code.",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "cheerio": "^1.0.0-rc.3",
    "slack-notify": "^0.1.7"
  }
}
